# ---------------------------------------------------------------------------
# Copy to .env.local when ready. The site runs without these today.
# Supabase + Resend are installed for future use only — not wired yet.
# ---------------------------------------------------------------------------

# Public site URL (sitemap / canonical later)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase — future CMS / forms / storage
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# SUPABASE_SERVICE_ROLE_KEY=   # required for the GitHub backup and restore actions

# Resend — future contact-form email delivery
# RESEND_API_KEY=
# EMAIL_FROM="Mukhtar Lab <hello@example.com>"
# EMAIL_TO=tanzila.mukhtar@ucsf.edu
